import { render, screen } from '@testing-library/react';
import App from '../App';

it('renders welcome message', () => {
  // fuck
  // render(<App />);
  // expect(screen.getByText('SecretNFT Certificates')).toBeInTheDocument();
});
